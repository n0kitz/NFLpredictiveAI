# NFL Prediction System Dependencies

# Web scraping
requests==2.32.5
beautifulsoup4==4.14.3
cloudscraper==1.2.71

# HTML parsing (optional faster parser)
lxml==5.2.1

# API server
fastapi==0.135.1
uvicorn[standard]==0.41.0
pydantic==2.12.5
slowapi==0.1.9

# Type checking (development)
typing-extensions==4.15.0

# Advanced stats integration
nfl-data-py==0.3.2
pandas==3.0.1

# Numerics — numpy pinned <2 to match the ABI of scipy 1.13 / scikit-learn 1.4.2,
# which are built against numpy 1.x. pandas 3.0 supports numpy 1.26+, so 1.26.x
# satisfies the whole stack. NOTE: the active anaconda base env may have numpy 2.x
# (causes "numpy.core.multiarray failed to import"); reinstall into a clean venv:
#   python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
numpy>=1.26,<2
scipy==1.13.1

# ML model
scikit-learn==1.4.2
joblib==1.4.2
shap==0.51.0

# Lineup optimizer (MILP)
pulp>=2.7.0

# Testing (development)
pytest==7.4.4
pytest-cov==7.1.0
